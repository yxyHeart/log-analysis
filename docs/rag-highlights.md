# LogScope RAG 亮点

- [1. Elasticsearch 原生混合检索](rag-highlights/01-hybrid-retrieval-es.md) — ES 8.x kNN + BM25 + RRF 单查询原生融合，告别双引擎拼接
- [2. LangGraph 状态机 + 质量门控](rag-highlights/02-langgraph-quality-gate.md) — 有状态图编排 + 条件反馈循环，检索不过关自动回退重写
- [3. 语义感知分块 + 向量稀释缓解](rag-highlights/03-semantic-chunking.md) — 三级优先分隔符 + 括号平衡 + 元数据注入，让每个 chunk 聚焦
- [4. RAGChecker 断言级评估](rag-highlights/04-ragchecker-evaluation.md) — 原子断言分解 + LLM 验证，超越文档级 recall 的量化体系
- [5. 全链路根因分析场景](rag-highlights/05-root-cause-scenario.md) — 微服务调用链故障知识库，结构化元数据驱动的精准检索

## 完整管线全景

```
                      SearchRequest
                      ┌──────────────────────────────────────────┐
                      │ query, top_k,                             │
                      │ use_hybrid, use_reranker,                 │
                      │ use_query_rewriting, llm_config,          │
                      │ metadata_filters                          │
                      └──────────────────┬───────────────────────┘
                                         │
                      ┌──────────────────▼───────────────────────┐
                      │         Query Analysis (QA)               │
                      │  ┌─────────────────────────────────────┐ │
                      │  │ LLM 意图分类 (error_lookup/          │ │
                      │  │   service_chain/category_browse/     │ │
                      │  │   symptom_match/general)              │ │
                      │  │ 结构化过滤提取 (services, category,  │ │
                      │  │   error_type, severity)               │ │
                      │  │ 查询重写 + 变体生成                    │ │
                      │  └─────────────────────────────────────┘ │
                      └──────────────────┬───────────────────────┘
                                         │
              ┌──────────────────────────┼────────────────────────┐
              │                          │                        │
     ┌────────▼────────┐    ┌───────────▼─────────┐  ┌──────────▼────────┐
     │  variant 1 检索  │    │  variant 2 检索      │  │  variant N 检索    │
     │ ┌─────────────┐ │    │ ┌─────────────────┐ │  │ ┌───────────────┐ │
     │ │  ES kNN 搜索 │ │    │ │  ES kNN 搜索    │ │  │ │  ES kNN 搜索   │ │
     │ │ (MiniLM 384d)│ │    │ │ (MiniLM 384d)   │ │  │ │ (MiniLM 384d)  │ │
     │ └──────┬──────┘ │    │ └────────┬────────┘ │  │ └───────┬───────┘ │
     │ ┌──────▼──────┐ │    │ ┌────────▼────────┐ │  │ ┌───────▼───────┐ │
     │ │  ES BM25     │ │    │ │  ES BM25        │ │  │ │  ES BM25      │ │
     │ │ (原生全文)   │ │    │ │  (原生全文)      │ │  │ │  (原生全文)    │ │
     │ └──────┬──────┘ │    │ └────────┬────────┘ │  │ └───────┬───────┘ │
     │ ┌──────▼──────┐ │    │ ┌────────▼────────┐ │  │ ┌───────▼───────┐ │
     │ │  ES RRF 融合 │ │    │ │  ES RRF 融合    │ │  │ │  ES RRF 融合   │ │
     │ │ (rank.rrf)   │ │    │ │  (rank.rrf)     │ │  │ │  (rank.rrf)    │ │
     │ └──────┬──────┘ │    │ └────────┬────────┘ │  │ └───────┬───────┘ │
     └────────┼────────┘    └──────────┼──────────┘  └─────────┼─────────┘
              │                        │                        │
              └────────────────────────┼────────────────────────┘
                                       │
                      ┌────────────────▼───────────────────────┐
                      │     合并去重 (all_candidates)            │
                      └────────────────┬───────────────────────┘
                                       │
                      ┌────────────────▼───────────────────────┐
                      │    Cross-encoder 重排 (RER)              │
                      │  ms-marco-MiniLM-L-12-v2                 │
                      │  top-50 → 精排 → top-k                   │
                      └────────────────┬───────────────────────┘
                                       │
                      ┌────────────────▼───────────────────────┐
                      │       Quality Gate (QG)                  │
                      │  ┌─────────────────────────────────────┐│
                      │  │ 质量评分 (0-1)                        ││
                      │  │ ┌─ top_score 归一化 × 0.5             ││
                      │  │ ├─ 分数间距因子 × 0.3                 ││
                      │  │ └─ 候选数因子 × 0.2                   ││
                      │  │ 阈值: quality >= 0.4 → 通过           ││
                      │  │ 动态K: 边界分数差 < 0.05 → 放宽 top-k  ││
                      │  └─────────────────────────────────────┘│
                      └────────┬──────────────┬─────────────────┘
                               │              │
                     passed ───┘              └─── not_passed
                               │                       │
                      ┌────────▼────────┐    ┌─────────▼────────────┐
                      │ Response Gen(RG)│    │ rewrite_attempts < 2? │
                      │ → 返回 top-k    │    ├─ yes → 回到 QA 节点   │
                      └─────────────────┘    │   (更激进的重写策略)    │
                                             └─ no  → 接受最佳结果    │
                                                → 进入 RG 节点       │
```

所有阶段通过 `SearchRequest` 参数独立控制：`use_hybrid`、`use_reranker`、`use_query_rewriting`、`metadata_filters`。
启用任一高级选项时自动使用 LangGraph 状态机管线；否则走轻量线性管线。
